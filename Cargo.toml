# Workspace root: the crate lives in `src/` (build with `cargo build --release`
# from the repo root or from inside `src/`).
[workspace]
members = ["src"]
resolver = "2"

[profile.release]
opt-level = 3
lto = "thin"